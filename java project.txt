import java.util.InputMismatchException;
import java.util.Scanner;

public class AdvancedTemperatureConverter {

    // Method to convert Celsius to Fahrenheit
    public static double celsiusToFahrenheit(double celsius) {
        return (celsius * 9 / 5) + 32;
    }

    // Method to convert Fahrenheit to Celsius
    public static double fahrenheitToCelsius(double fahrenheit) {
        return (fahrenheit - 32) * 5 / 9;
    }

    // Method to convert Celsius to Kelvin
    public static double celsiusToKelvin(double celsius) {
        return celsius + 273.15;
    }

    // Method to convert Kelvin to Celsius
    public static double kelvinToCelsius(double kelvin) {
        return kelvin - 273.15;
    }

    // Method to convert Fahrenheit to Kelvin
    public static double fahrenheitToKelvin(double fahrenheit) {
        return (fahrenheit - 32) * 5 / 9 + 273.15;
    }

    // Method to convert Kelvin to Fahrenheit
    public static double kelvinToFahrenheit(double kelvin) {
        return (kelvin - 273.15) * 9 / 5 + 32;
    }

    // Method to get a valid double input from the user
    private static double getValidDoubleInput(Scanner scanner) {
        double value;
        while (true) {
            try {
                value = scanner.nextDouble();
                break;
            } catch (InputMismatchException e) {
                System.out.print("Invalid input. Please enter a valid number: ");
                scanner.next(); // Clear the invalid input
            }
        }
        return value;
    }

    // Method to get a valid integer input from the user
    private static int getValidIntInput(Scanner scanner, int min, int max) {
        int choice;
        while (true) {
            try {
                choice = scanner.nextInt();
                if (choice >= min && choice <= max) {
                    break;
                } else {
                    System.out.print("Invalid choice. Please choose a valid option (" + min + "-" + max + "): ");
                }
            } catch (InputMismatchException e) {
                System.out.print("Invalid input. Please enter a valid option (" + min + "-" + max + "): ");
                scanner.next(); // Clear the invalid input
            }
        }
        return choice;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        while (true) {
            System.out.println("\nTemperature Converter Menu:");
            System.out.println("1. Celsius to Fahrenheit");
            System.out.println("2. Fahrenheit to Celsius");
            System.out.println("3. Celsius to Kelvin");
            System.out.println("4. Kelvin to Celsius");
            System.out.println("5. Fahrenheit to Kelvin");
            System.out.println("6. Kelvin to Fahrenheit");
            System.out.println("7. Exit");
            System.out.print("Choose an option (1-7): ");

            int choice = getValidIntInput(scanner, 1, 7);

            if (choice == 7) {
                System.out.println("Exiting the program. Goodbye!");
                break;
            }

            System.out.print("Enter the temperature to convert: ");
            double inputTemperature = getValidDoubleInput(scanner);
            double resultTemperature = 0;

            switch (choice) {
                case 1:
                    resultTemperature = celsiusToFahrenheit(inputTemperature);
                    System.out.printf("%.2f Celsius is %.2f Fahrenheit.%n", inputTemperature, resultTemperature);
                    break;
                case 2:
                    resultTemperature = fahrenheitToCelsius(inputTemperature);
                    System.out.printf("%.2f Fahrenheit is %.2f Celsius.%n", inputTemperature, resultTemperature);
                    break;
                case 3:
                    resultTemperature = celsiusToKelvin(inputTemperature);
                    System.out.printf("%.2f Celsius is %.2f Kelvin.%n", inputTemperature, resultTemperature);
                    break;
                case 4:
                    resultTemperature = kelvinToCelsius(inputTemperature);
                    System.out.printf("%.2f Kelvin is %.2f Celsius.%n", inputTemperature, resultTemperature);
                    break;
                case 5:
                    resultTemperature = fahrenheitToKelvin(inputTemperature);
                    System.out.printf("%.2f Fahrenheit is %.2f Kelvin.%n", inputTemperature, resultTemperature);
                    break;
                case 6:
                    resultTemperature = kelvinToFahrenheit(inputTemperature);
                    System.out.printf("%.2f Kelvin is %.2f Fahrenheit.%n", inputTemperature, resultTemperature);
                    break;
                default:
                    System.out.println("Invalid choice. Please choose a valid option.");
                    break;
            }
        }
        scanner.close();
    }
}